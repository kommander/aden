describe('Aden Plugin API', () => {
  she('provides hooks');
  she('...describes hooks per core module');
  she('allows to extend the client core');
  she('loads no plugins per default');
  she('loads plugins in the order specified in .aden');
  she('monitors what plugins add to the core page');
  she('creates a documented page config schema with all added features');
});
