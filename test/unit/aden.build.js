describe('Aden Build', () => {
  //       >> like dev/prod build, what plugins were used, info from compiler stats
  //       >> To run aden on a .dist build without parsing in production
  she('exports some build info json with dist for packaging');
});
